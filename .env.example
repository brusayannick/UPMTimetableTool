# Copy to `.env` (never commit it). Only needed for `npm run supabase:seed`;
# the app itself reads the static `public/bundle.json` and needs no env vars.
SUPABASE_URL=https://xyzcompany.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...
